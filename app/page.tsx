import { Stack } from "@mui/material";

export default function Home() {
  return (
    <Stack
      sx={{
        height: "100%",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      Unblink Landing Page
    </Stack>
  );
}
